apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "authn-example.fullname" . }}-test-connection"
  labels:
    {{- include "authn-example.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "authn-example.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
